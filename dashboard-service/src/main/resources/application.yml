server:
  port: 8082

spring:
  application:
    name: dashboard-service

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:eureka.client.service-url.defaultZone=${EUREKA_SERVER}}
